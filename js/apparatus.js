/**
 * @file
 * Ties links in Apparatus block to vertical tab display.
 */
(function ($) {
  "use strict";
  Drupal.behaviors.apparatus = {
    attach: function (context, settings) {
      // Link each apparatus component link in the block to the proper fieldset.
    }
  };
}(jQuery));